[project]
name = "weixin-gateway-fastapi-demo"
version = "0.1.0"
description = "Minimal FastAPI demo for weixin-gateway"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116.0",
  "httpx>=0.28.0",
  "qrcode>=8.2",
  "uvicorn>=0.35.0",
]

[tool.uv]
package = false
